# Local dev defaults — point at the Postgres instance already running on your machine.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=admin
DB_NAME=bit_fluencer

PORT=4000
WEB_ORIGIN=http://localhost:3000
